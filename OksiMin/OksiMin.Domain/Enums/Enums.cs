namespace OksiMin.Domain.Enums
{
    public enum PlaceStatus
    {
        Approved = 1,
        Archived = 2
    }

    public enum SubmissionStatus
    {
        Pending = 1,
        Approved = 2,
        Rejected = 3
    }

    public enum UserRole
    {
        Admin = 1,
        Moderator = 2
    }
}